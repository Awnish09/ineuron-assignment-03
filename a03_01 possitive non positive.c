
#include<stdio.h>
#include<conio.h>

int main()
{
    int a=0;
    if(a>0)
        printf("%d is possitive.\n",a);
    if(a<=0)
        printf("%d is non positive.",a);

    getch();

    return 0;
}
