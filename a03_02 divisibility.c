
#include<stdio.h>
#include<conio.h>

int main()
{
    int a=10;
    if(a%5)
        printf("%d is not divisible by 5.\n",a);
    else
        printf("%d is divisible by 5.",a);

    getch();

    return 0;
}
